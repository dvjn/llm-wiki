---
description: Initialize a new wiki in this project
---

Initialize a new LLM wiki in this project.

Read `${CLAUDE_PLUGIN_ROOT}/skills/llm-wiki/references/wiki-setup.md` and follow it exactly: create the bootstrap directory structure and starter files, then add the AGENTS.md wiki section using only the template at `${CLAUDE_PLUGIN_ROOT}/skills/llm-wiki/references/templates/agents-md-wiki-section.md`.

If a `wiki/` directory already exists, stop and report that instead of re-initializing.
